package com.ho.practice.jpa.querydsl.support;

import org.springframework.data.jpa.repository.JpaRepository;

public interface AccountDslSupportRepository extends JpaRepository<AccountDslSupport, Long> {
}
